export enum AppRoutes {
  Catalog = '/',
  Product = 'product/:cameraId',
  Basket = 'basket',
}

export enum FetchRoutes {
  Catalog = '/cameras',
  Product = '/cameras',
  Similar = '/similar',
  Promo = '/promo',
  Reviews = '/reviews',
  Coupons = '/coupons',
  Orders = '/orders',
}
