export enum AppRoutes {
  Catalog = '/',
  Camera = 'camera',
  Basket = 'basket',
}

export enum FetchRoutes {
  Catalog = '/cameras',
  Camera = '/cameras',
  Similar = '/similar',
  Promo = '/promo',
  Reviews = '/reviews',
  Coupons = '/coupons',
  Orders = '/orders',
}
